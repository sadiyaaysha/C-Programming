#include <stdio.h>
#include<string.h>
int main(void) {
	// your code goes here
	int i;
	char a[10];
	char temp;
	scanf("%s",a);

	for(i=0;i<strlen(a);i=i+2)
	{
	if(a[i+1]==NULL)
		{
		continue;
		}
		temp=a[i];
		a[i]=a[i+1];
		a[i+1]=temp;
		
			
	}
	printf("%s",a);
	return 0;
}

